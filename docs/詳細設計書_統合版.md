# Google Contacts 連絡先移行ツール - 統合版 詳細設計書

> 注記: この文書の一部コード例は初期実装時点のものです。公開版では `invoke_gam.ps1` による引数配列実行、UI ログキュー化、`contacts.csv` 命名、旧単体 GUI の公開対象外化が反映されています。現行挙動はソースコードを優先してください。

## 目次
1. [Phase 1: 統合GUIアプリケーション](#phase-1-統合guiアプリケーション)
2. [Phase 2: Gitbash検出・GAMコマンド実行](#phase-2-gitbash検出gamコマンド実行)
3. [Phase 3: Step 2 - CSV抽出](#phase-3-step-2---csv抽出)
4. [Phase 4: Step 3 - 連絡先登録](#phase-4-step-3---連絡先登録)
5. [Phase 5: Step 4 - ラベル作成](#phase-5-step-4---ラベル作成)
6. [Phase 6: Step 5 - 登録データ取得](#phase-6-step-5---登録データ取得)
7. [Phase 7: Step 6 - データ突合](#phase-7-step-6---データ突合)
8. [Phase 8: Step 7 - ラベル登録](#phase-8-step-7---ラベル登録)

---

# Phase 1: 統合GUIアプリケーション

## 1.1 概要
統合GUIアプリケーション（`transferContacts_master.py`）のUI設計と初期化処理。

**実装ファイル**: `transferContacts_master.py`

---

## 1.2 クラス設計

### 1.2.1 TransferContactsMasterGUI クラス

**責務**: 統合GUIアプリケーションのメインクラス

**属性**:
```python
class TransferContactsMasterGUI:
    # GUI要素
    root: tk.Tk                         # メインウィンドウ

    # ファイルパス変数
    export_csv_path: tk.StringVar       # エクスポートCSVパス
    target_email: tk.StringVar          # ターゲットメールアドレス
    contact_csv_path: tk.StringVar      # 連絡先CSV出力先
    label_csv_path: tk.StringVar        # ラベルCSV出力先
    log_dir: tk.StringVar               # ログ出力先

    # 処理ステップのチェックボックス
    step2_enabled: tk.BooleanVar        # Step 2有効/無効
    step3_enabled: tk.BooleanVar        # Step 3有効/無効
    step4_enabled: tk.BooleanVar        # Step 4有効/無効
    step5_enabled: tk.BooleanVar        # Step 5有効/無効
    step6_enabled: tk.BooleanVar        # Step 6有効/無効
    step7_enabled: tk.BooleanVar        # Step 7有効/無効

    # 実行時変数
    work_dir: Path                      # 作業ディレクトリ
    gitbash_path: str                   # Gitbashのパス

    # 中間ファイル保存先
    intermediate_contact_csv: Path      # 連絡先CSV（Step 2出力）
    intermediate_label_csv: Path        # ラベルCSV（Step 2出力）
    intermediate_contacts_csv: Path    # 登録済み連絡先CSV（Step 5出力）
    intermediate_contactgroups_csv: Path # 登録済みラベルCSV（Step 5出力）
    final_labels_csv: Path              # ラベル登録CSV（Step 6出力）

    # UI要素
    log_text: scrolledtext.ScrolledText # ログ表示エリア
    process_button: tk.Button           # 処理開始ボタン
```

---

## 1.3 メソッド設計

### 1.3.1 `__init__(self, root)`
**責務**: GUIの初期化

**処理フロー**:
1. メインウィンドウ設定
2. 変数の初期化
3. Gitbash検出
4. UI構築

**実装コード**:
```python
def __init__(self, root):
    """初期化"""
    self.root = root
    self.root.title("Google Contacts 連絡先移行ツール - 統合版")
    self.root.geometry("1000x900")

    # ファイルパス変数
    self.export_csv_path = tk.StringVar()
    self.target_email = tk.StringVar()
    self.contact_csv_path = tk.StringVar()
    self.label_csv_path = tk.StringVar()
    self.log_dir = tk.StringVar(value="./logs")

    # 処理ステップ有効/無効
    self.step2_enabled = tk.BooleanVar(value=True)
    self.step3_enabled = tk.BooleanVar(value=True)
    self.step4_enabled = tk.BooleanVar(value=True)
    self.step5_enabled = tk.BooleanVar(value=True)
    self.step6_enabled = tk.BooleanVar(value=True)
    self.step7_enabled = tk.BooleanVar(value=True)

    # 中間ファイル
    self.work_dir = None
    self.intermediate_contact_csv = None
    self.intermediate_label_csv = None
    self.intermediate_contacts_csv = None
    self.intermediate_contactgroups_csv = None
    self.final_labels_csv = None

    # Gitbash検出
    self.gitbash_path = self.find_gitbash()

    # UI構築
    self.create_widgets()

    # 初期ログ出力
    if self.gitbash_path:
        self.log(f"✅ Gitbash検出: {self.gitbash_path}")
    else:
        self.log("⚠️ Gitbash未検出、PowerShellで代替実行します")
        messagebox.showwarning(
            "警告",
            "Gitbash (bash.exe) が見つかりませんでした。\n"
            "GAMコマンドの文字化けが発生する可能性があります。\n"
            "Git for Windowsのインストールを推奨します。"
        )
```

---

### 1.3.2 `create_widgets(self)`
**責務**: UI要素の構築

**UI構成**:
```
┌─────────────────────────────────────────────────────────┐
│  Google Contacts 連絡先移行ツール - 統合版              │
├─────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────┐  │
│  │ ① 入力ファイル選択                                │  │
│  │  ─────────────────────────────────────────────  │  │
│  │  エクスポートCSV: [入力欄           ] [参照...] │  │
│  │  ターゲットメール: [入力欄          ]           │  │
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │ ② 出力先設定（任意、未指定時は自動生成）        │  │
│  │  ─────────────────────────────────────────────  │  │
│  │  連絡先CSV: [入力欄                ] [参照...]  │  │
│  │  ラベルCSV: [入力欄                ] [参照...]  │  │
│  │  ログ出力先: [./logs/              ] [参照...]  │  │
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │ ③ 処理ステップ選択                               │  │
│  │  ─────────────────────────────────────────────  │  │
│  │  ☑ Step 2: CSV抽出                              │  │
│  │  ☑ Step 3: 連絡先登録                           │  │
│  │  ☑ Step 4: ラベル作成                           │  │
│  │  ☑ Step 5: 登録データ取得                       │  │
│  │  ☑ Step 6: データ突合                           │  │
│  │  ☑ Step 7: ラベル登録                           │  │
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │ [設定を確認]  [処理開始 ▶]  [クリア]            │  │
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │ 実行ログ                                          │  │
│  │ ─────────────────────────────────────────────   │  │
│  │ [2025/12/30 14:30:22] ✅ Gitbash検出: ...       │  │
│  │ [2025/12/30 14:30:25] === 処理開始 ===          │  │
│  │ [2025/12/30 14:30:26] --- Step 2: CSV抽出 ---   │  │
│  │ ...                                              │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

**実装コード**:
```python
def create_widgets(self):
    """UI構築"""
    # タイトル
    title_frame = tk.Frame(self.root, bg='#2c3e50', pady=15)
    title_frame.pack(fill=tk.X)

    title_label = tk.Label(
        title_frame,
        text="Google Contacts 連絡先移行ツール - 統合版",
        font=('Yu Gothic UI', 16, 'bold'),
        bg='#2c3e50',
        fg='white'
    )
    title_label.pack()

    # メインフレーム
    main_frame = tk.Frame(self.root, padx=20, pady=20)
    main_frame.pack(fill=tk.BOTH, expand=True)

    # ① 入力ファイル選択セクション
    input_section = tk.LabelFrame(
        main_frame,
        text="① 入力ファイル選択",
        padx=15,
        pady=15,
        font=('Yu Gothic UI', 10, 'bold')
    )
    input_section.pack(fill=tk.X, pady=(0, 10))

    # エクスポートCSV
    tk.Label(
        input_section,
        text="エクスポートCSV:",
        width=20,
        anchor=tk.W
    ).grid(row=0, column=0, sticky=tk.W, pady=5)

    tk.Entry(
        input_section,
        textvariable=self.export_csv_path,
        width=50
    ).grid(row=0, column=1, sticky=tk.EW, padx=(0, 10), pady=5)

    tk.Button(
        input_section,
        text="参照...",
        command=lambda: self.browse_file(self.export_csv_path, "エクスポートCSVを選択")
    ).grid(row=0, column=2, pady=5)

    # ターゲットメールアドレス
    tk.Label(
        input_section,
        text="ターゲットメールアドレス:",
        width=20,
        anchor=tk.W
    ).grid(row=1, column=0, sticky=tk.W, pady=5)

    tk.Entry(
        input_section,
        textvariable=self.target_email,
        width=50
    ).grid(row=1, column=1, sticky=tk.EW, pady=5)

    input_section.columnconfigure(1, weight=1)

    # ② 出力先設定セクション
    output_section = tk.LabelFrame(
        main_frame,
        text="② 出力先設定（任意、未指定時は自動生成）",
        padx=15,
        pady=15,
        font=('Yu Gothic UI', 10, 'bold')
    )
    output_section.pack(fill=tk.X, pady=(0, 10))

    # 連絡先CSV出力先
    tk.Label(
        output_section,
        text="連絡先CSV:",
        width=20,
        anchor=tk.W
    ).grid(row=0, column=0, sticky=tk.W, pady=5)

    tk.Entry(
        output_section,
        textvariable=self.contact_csv_path,
        width=50
    ).grid(row=0, column=1, sticky=tk.EW, padx=(0, 10), pady=5)

    tk.Button(
        output_section,
        text="参照...",
        command=lambda: self.browse_save_file(
            self.contact_csv_path,
            "連絡先CSV保存先を選択",
            "連絡先"
        )
    ).grid(row=0, column=2, pady=5)

    # ラベルCSV出力先
    tk.Label(
        output_section,
        text="ラベルCSV:",
        width=20,
        anchor=tk.W
    ).grid(row=1, column=0, sticky=tk.W, pady=5)

    tk.Entry(
        output_section,
        textvariable=self.label_csv_path,
        width=50
    ).grid(row=1, column=1, sticky=tk.EW, padx=(0, 10), pady=5)

    tk.Button(
        output_section,
        text="参照...",
        command=lambda: self.browse_save_file(
            self.label_csv_path,
            "ラベルCSV保存先を選択",
            "ラベル"
        )
    ).grid(row=1, column=2, pady=5)

    # ログ出力先
    tk.Label(
        output_section,
        text="ログ出力先:",
        width=20,
        anchor=tk.W
    ).grid(row=2, column=0, sticky=tk.W, pady=5)

    tk.Entry(
        output_section,
        textvariable=self.log_dir,
        width=50
    ).grid(row=2, column=1, sticky=tk.EW, padx=(0, 10), pady=5)

    tk.Button(
        output_section,
        text="参照...",
        command=lambda: self.browse_directory(self.log_dir, "ログ出力先を選択")
    ).grid(row=2, column=2, pady=5)

    output_section.columnconfigure(1, weight=1)

    # ③ 処理ステップ選択セクション
    step_section = tk.LabelFrame(
        main_frame,
        text="③ 処理ステップ選択",
        padx=15,
        pady=15,
        font=('Yu Gothic UI', 10, 'bold')
    )
    step_section.pack(fill=tk.X, pady=(0, 10))

    tk.Checkbutton(
        step_section,
        text="Step 2: CSV抽出",
        variable=self.step2_enabled,
        font=('Yu Gothic UI', 10)
    ).grid(row=0, column=0, sticky=tk.W, pady=2)

    tk.Checkbutton(
        step_section,
        text="Step 3: 連絡先登録",
        variable=self.step3_enabled,
        font=('Yu Gothic UI', 10)
    ).grid(row=1, column=0, sticky=tk.W, pady=2)

    tk.Checkbutton(
        step_section,
        text="Step 4: ラベル作成",
        variable=self.step4_enabled,
        font=('Yu Gothic UI', 10)
    ).grid(row=2, column=0, sticky=tk.W, pady=2)

    tk.Checkbutton(
        step_section,
        text="Step 5: 登録データ取得",
        variable=self.step5_enabled,
        font=('Yu Gothic UI', 10)
    ).grid(row=0, column=1, sticky=tk.W, pady=2, padx=(30, 0))

    tk.Checkbutton(
        step_section,
        text="Step 6: データ突合",
        variable=self.step6_enabled,
        font=('Yu Gothic UI', 10)
    ).grid(row=1, column=1, sticky=tk.W, pady=2, padx=(30, 0))

    tk.Checkbutton(
        step_section,
        text="Step 7: ラベル登録",
        variable=self.step7_enabled,
        font=('Yu Gothic UI', 10)
    ).grid(row=2, column=1, sticky=tk.W, pady=2, padx=(30, 0))

    # ボタンフレーム
    button_frame = tk.Frame(main_frame)
    button_frame.pack(fill=tk.X, pady=(10, 10))

    # 設定を確認ボタン
    tk.Button(
        button_frame,
        text="設定を確認",
        command=self.confirm_settings,
        bg='#3498db',
        fg='white',
        font=('Yu Gothic UI', 11, 'bold'),
        padx=20,
        pady=8
    ).pack(side=tk.LEFT, padx=(0, 10))

    # 処理開始ボタン
    self.process_button = tk.Button(
        button_frame,
        text="処理開始 ▶",
        command=self.confirm_and_run,
        bg='#27ae60',
        fg='white',
        font=('Yu Gothic UI', 12, 'bold'),
        padx=30,
        pady=8
    )
    self.process_button.pack(side=tk.LEFT, padx=(0, 10))

    # クリアボタン
    tk.Button(
        button_frame,
        text="クリア",
        command=self.clear_all,
        bg='#e74c3c',
        fg='white',
        font=('Yu Gothic UI', 11),
        padx=20,
        pady=8
    ).pack(side=tk.LEFT)

    # ログ出力エリア
    log_section = tk.LabelFrame(
        main_frame,
        text="実行ログ",
        padx=10,
        pady=10,
        font=('Yu Gothic UI', 10, 'bold')
    )
    log_section.pack(fill=tk.BOTH, expand=True)

    self.log_text = scrolledtext.ScrolledText(
        log_section,
        height=20,
        bg='#1e1e1e',
        fg='#00ff00',
        font=('Consolas', 9),
        wrap=tk.WORD
    )
    self.log_text.pack(fill=tk.BOTH, expand=True)
```

---

### 1.3.3 ファイル選択関連メソッド

#### `browse_file(self, path_var, title)`
**責務**: ファイル選択ダイアログを表示

```python
def browse_file(self, path_var, title):
    """ファイル選択ダイアログ"""
    filename = filedialog.askopenfilename(
        title=title,
        filetypes=[("CSV files", "*.csv"), ("All files", "*.*")]
    )
    if filename:
        path_var.set(filename)
        self.log(f"ファイル選択: {filename}")
```

#### `browse_save_file(self, path_var, title, default_name)`
**責務**: 保存ファイル選択ダイアログを表示

```python
def browse_save_file(self, path_var, title, default_name):
    """保存ファイル選択ダイアログ"""
    from datetime import datetime

    timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
    default_filename = f"{default_name}_{timestamp}.csv"

    filename = filedialog.asksaveasfilename(
        title=title,
        defaultextension=".csv",
        initialfile=default_filename,
        filetypes=[("CSV files", "*.csv"), ("All files", "*.*")]
    )
    if filename:
        path_var.set(filename)
        self.log(f"保存先選択: {filename}")
```

#### `browse_directory(self, path_var, title)`
**責務**: ディレクトリ選択ダイアログを表示

```python
def browse_directory(self, path_var, title):
    """ディレクトリ選択ダイアログ"""
    dirname = filedialog.askdirectory(title=title)
    if dirname:
        path_var.set(dirname)
        self.log(f"ディレクトリ選択: {dirname}")
```

---

### 1.3.4 `log(self, message)`
**責務**: ログメッセージをGUIに追加

```python
def log(self, message):
    """ログメッセージ追加"""
    from datetime import datetime

    timestamp = datetime.now().strftime('%Y/%m/%d %H:%M:%S')
    log_line = f"[{timestamp}] {message}"

    self.log_text.insert(tk.END, log_line + '\n')
    self.log_text.see(tk.END)
    self.root.update_idletasks()
```

---

### 1.3.5 `clear_all(self)`
**責務**: すべての入力フィールドをクリア

```python
def clear_all(self):
    """すべてのフィールドをクリア"""
    self.export_csv_path.set('')
    self.target_email.set('')
    self.contact_csv_path.set('')
    self.label_csv_path.set('')
    self.log_dir.set('./logs')

    # チェックボックスを全てON
    self.step2_enabled.set(True)
    self.step3_enabled.set(True)
    self.step4_enabled.set(True)
    self.step5_enabled.set(True)
    self.step6_enabled.set(True)
    self.step7_enabled.set(True)

    # ログクリア
    self.log_text.delete(1.0, tk.END)
    self.log('クリア完了')
```

---

### 1.3.6 `confirm_settings(self)`
**責務**: 設定内容を表示（確認用）

```python
def confirm_settings(self):
    """設定内容を表示"""
    message = self.generate_confirmation_message()

    messagebox.showinfo(
        "設定内容の確認",
        message,
        icon='info'
    )
```

---

### 1.3.7 `generate_confirmation_message(self)`
**責務**: 確認メッセージを生成

```python
def generate_confirmation_message(self):
    """確認メッセージ生成"""
    enabled_steps = []
    if self.step2_enabled.get():
        enabled_steps.append("✅ Step 2: CSV抽出")
    if self.step3_enabled.get():
        enabled_steps.append("✅ Step 3: 連絡先登録")
    if self.step4_enabled.get():
        enabled_steps.append("✅ Step 4: ラベル作成")
    if self.step5_enabled.get():
        enabled_steps.append("✅ Step 5: 登録データ取得")
    if self.step6_enabled.get():
        enabled_steps.append("✅ Step 6: データ突合")
    if self.step7_enabled.get():
        enabled_steps.append("✅ Step 7: ラベル登録")

    steps_text = "\n  ".join(enabled_steps)

    message = f"""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  実行設定の確認
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【入力】
  エクスポートCSV: {self.export_csv_path.get() or '(未選択)'}
  ターゲットメール: {self.target_email.get() or '(未入力)'}

【出力先】
  連絡先CSV: {self.contact_csv_path.get() or '(自動生成)'}
  ラベルCSV: {self.label_csv_path.get() or '(自動生成)'}
  ログ: {self.log_dir.get()}

【実行する処理】
  {steps_text}
"""
    return message
```

---

### 1.3.8 `confirm_and_run(self)`
**責務**: 実行確認ダイアログを表示し、処理開始

```python
def confirm_and_run(self):
    """実行確認ダイアログ表示"""
    # バリデーション
    if not self.export_csv_path.get():
        messagebox.showerror("エラー", "エクスポートCSVを選択してください")
        return

    if not self.target_email.get():
        messagebox.showerror("エラー", "ターゲットメールアドレスを入力してください")
        return

    # メールアドレス形式チェック
    import re
    email_pattern = r'^[^\s@]+@[^\s@]+\.[^\s@]+$'
    if not re.match(email_pattern, self.target_email.get()):
        messagebox.showerror("エラー", "メールアドレスの形式が正しくありません")
        return

    # 確認メッセージ
    confirm_msg = self.generate_confirmation_message()
    confirm_msg += "\n\n処理を開始してよろしいですか？"

    result = messagebox.askyesno(
        "実行確認",
        confirm_msg,
        icon='question'
    )

    if result:
        self.run_all_steps()
```

---

## 1.4 テストケース

### 1.4.1 正常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-1.1: 初期表示 | アプリ起動 | GUI正常表示、Gitbash検出メッセージ表示 |
| TC-1.2: ファイル選択 | 参照ボタンクリック | ファイル選択ダイアログ表示 |
| TC-1.3: 設定確認 | 設定を確認ボタンクリック | 確認メッセージダイアログ表示 |
| TC-1.4: クリア | クリアボタンクリック | すべてのフィールドがクリアされる |

### 1.4.2 異常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-1.5: 未入力エラー | エクスポートCSV未選択で実行 | エラーダイアログ表示 |
| TC-1.6: メールアドレス形式エラー | 不正なメールアドレス入力 | エラーダイアログ表示 |
| TC-1.7: Gitbash未検出 | Gitbashなし環境 | 警告ダイアログ表示、処理は継続 |

---

# Phase 2: Gitbash検出・GAMコマンド実行

## 2.1 概要
Gitbashの自動検出とGAMコマンドの実行機能。文字化け対策として、Gitbash経由でGAMコマンドを実行する。

---

## 2.2 メソッド設計

### 2.2.1 `find_gitbash(self)`
**責務**: Gitbash (bash.exe) のパスを検索

**処理フロー**:
1. 一般的なインストールパスを検索
2. 見つかった場合はパスを返す
3. 見つからない場合はNoneを返す

**実装コード**:
```python
def find_gitbash(self):
    """Gitbashのパスを検索"""
    from pathlib import Path

    gitbash_paths = [
        r"C:\Program Files\Git\bin\bash.exe",
        r"C:\Program Files (x86)\Git\bin\bash.exe",
        Path.home() / "AppData" / "Local" / "Programs" / "Git" / "bin" / "bash.exe"
    ]

    for path in gitbash_paths:
        if Path(path).exists():
            return str(path)

    return None
```

---

### 2.2.2 `run_gam_via_gitbash(self, gam_command)`
**責務**: Gitbash経由でGAMコマンドを実行

**引数**:
- `gam_command` (str): 実行するGAMコマンド（例: "gam user xxx print contactgroups"）

**戻り値**:
- `str`: コマンド実行結果（UTF-8文字列）

**処理フロー**:
1. Gitbashパスが存在するかチェック
2. 存在しない場合はPowerShell版にフォールバック
3. Gitbash経由でGAMコマンド実行
4. 結果をUTF-8文字列として返す

**実装コード**:
```python
def run_gam_via_gitbash(self, gam_command):
    """Gitbash経由でGAMコマンド実行"""
    import subprocess

    if not self.gitbash_path:
        # フォールバック: PowerShell UTF-8版を使用
        self.log("⚠️ Gitbash未検出、PowerShellで実行します")
        return self.run_gam_via_powershell_utf8(gam_command)

    cmd = [self.gitbash_path, "-c", gam_command]

    try:
        result = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            encoding='utf-8',
            errors='replace',
            timeout=600  # 10分タイムアウト
        )

        if result.returncode != 0:
            raise Exception(f"GAMコマンド実行失敗: {result.stderr}")

        return result.stdout

    except subprocess.TimeoutExpired:
        raise Exception("GAMコマンドがタイムアウトしました（10分以上）")
    except Exception as e:
        raise Exception(f"GAMコマンド実行エラー: {str(e)}")
```

---

### 2.2.3 `run_gam_via_powershell_utf8(self, gam_command)`
**責務**: PowerShell UTF-8版でGAMコマンドを実行（フォールバック）

**引数**:
- `gam_command` (str): 実行するGAMコマンド

**戻り値**:
- `str`: コマンド実行結果（UTF-8文字列）

**実装コード**:
```python
def run_gam_via_powershell_utf8(self, gam_command):
    """PowerShellでGAMコマンド実行（UTF-8エンコーディング強制）"""
    import subprocess

    ps_script = f"""
    # UTF-8エンコーディング設定
    $OutputEncoding = [System.Text.Encoding]::UTF8
    [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
    $PSDefaultParameterValues['*:Encoding'] = 'utf8'

    # GAMコマンド実行
    {gam_command}
    """

    cmd = [
        "powershell.exe",
        "-NoProfile",
        "-ExecutionPolicy", "Bypass",
        "-Command", ps_script
    ]

    try:
        result = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            encoding='utf-8',
            errors='replace',
            timeout=600
        )

        if result.returncode != 0:
            raise Exception(f"GAMコマンド実行失敗: {result.stderr}")

        return result.stdout

    except subprocess.TimeoutExpired:
        raise Exception("GAMコマンドがタイムアウトしました（10分以上）")
    except Exception as e:
        raise Exception(f"GAMコマンド実行エラー: {str(e)}")
```

---

## 2.3 テストケース

### 2.3.1 正常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-2.1: Gitbash検出成功 | Gitbashインストール済み | パスが返される、ログに成功メッセージ |
| TC-2.2: GAMコマンド実行成功 | 有効なGAMコマンド | コマンド結果がUTF-8文字列で返される |
| TC-2.3: 日本語ラベル名対応 | 日本語を含むラベル | 文字化けせずに取得できる |

### 2.3.2 異常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-2.4: Gitbash未検出 | Gitbash未インストール | Noneが返される、警告ダイアログ表示 |
| TC-2.5: GAMコマンドエラー | 不正なGAMコマンド | 例外発生、エラーメッセージ表示 |
| TC-2.6: タイムアウト | 長時間実行コマンド | タイムアウト例外発生 |

---

# Phase 3: Step 2 - CSV抽出

## 3.1 概要
エクスポートCSVから連絡先CSVとラベルCSVを生成する。既存の`convert_contacts.py`を呼び出す。

---

## 3.2 メソッド設計

### 3.2.1 `execute_step2(self)`
**責務**: Step 2: CSV抽出を実行

**処理フロー**:
1. 出力先パス決定（未指定時は自動生成）
2. Pythonスクリプト実行
3. 結果確認
4. 中間ファイルパスを保存

**実装コード**:
```python
def execute_step2(self):
    """Step 2: CSV抽出"""
    import subprocess
    from pathlib import Path

    self.log("--- Step 2: CSV抽出 開始 ---")

    try:
        # 出力先決定
        if self.contact_csv_path.get():
            contact_csv = Path(self.contact_csv_path.get())
        else:
            contact_csv = self.work_dir / "連絡先.csv"

        if self.label_csv_path.get():
            label_csv = Path(self.label_csv_path.get())
        else:
            label_csv = self.work_dir / "ラベル.csv"

        self.log(f"  入力: {self.export_csv_path.get()}")
        self.log(f"  出力: {contact_csv}, {label_csv}")

        # Python実行
        cmd = [
            "python",
            "csvContactsFirst/convert_contacts.py",
            self.export_csv_path.get(),
            str(contact_csv),
            str(label_csv),
            "--log-level", "INFO"
        ]

        result = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            encoding='utf-8',
            timeout=300  # 5分タイムアウト
        )

        if result.returncode == 0:
            self.log("  " + result.stdout.replace('\n', '\n  '))
            self.log("✅ Step 2: 完了")

            # 中間ファイルパスを保存
            self.intermediate_contact_csv = contact_csv
            self.intermediate_label_csv = label_csv
        else:
            self.log(f"❌ Step 2: 失敗")
            self.log(f"  エラー: {result.stderr}")
            raise Exception(f"Step 2 失敗: {result.stderr}")

    except subprocess.TimeoutExpired:
        self.log("❌ Step 2: タイムアウト（5分以上）")
        raise Exception("Step 2がタイムアウトしました")
    except Exception as e:
        self.log(f"❌ Step 2: エラー - {str(e)}")
        raise
```

---

## 3.3 テストケース

### 3.3.1 正常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-3.1: CSV抽出成功 | 有効なエクスポートCSV | 連絡先CSVとラベルCSVが生成される |
| TC-3.2: 出力先指定 | 任意の出力先指定 | 指定したパスにファイル生成 |
| TC-3.3: 自動生成 | 出力先未指定 | work_dirに自動生成 |

### 3.3.2 異常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-3.4: ファイル不存在 | 存在しないエクスポートCSV | エラーメッセージ表示、処理中断 |
| TC-3.5: タイムアウト | 大量データ処理 | タイムアウト例外発生 |

---

# Phase 4: Step 3 - 連絡先登録

## 4.1 概要
連絡先CSVからJSON形式に変換し、GAMコマンドで一括登録する。PowerShell CLI版を呼び出す。

---

## 4.2 メソッド設計

### 4.2.1 `execute_step3(self)`
**責務**: Step 3: 連絡先登録を実行

**処理フロー**:
1. PowerShell CLI版スクリプトを呼び出し
2. 連絡先CSVとターゲットメールを引数として渡す
3. 実行結果をログに表示

**実装コード**:
```python
def execute_step3(self):
    """Step 3: 連絡先登録（PowerShell呼び出し）"""
    import subprocess

    self.log("--- Step 3: 連絡先登録 開始 ---")

    try:
        self.log(f"  連絡先CSV: {self.intermediate_contact_csv}")
        self.log(f"  ターゲットメール: {self.target_email.get()}")

        # PowerShellスクリプトを呼び出し
        cmd = [
            "powershell.exe",
            "-ExecutionPolicy", "Bypass",
            "-File", "registerContacts/run_register_cli.ps1",
            "-ContactCsvFile", str(self.intermediate_contact_csv),
            "-TargetUserEmail", self.target_email.get(),
            "-LogDir", self.log_dir.get()
        ]

        result = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            encoding='utf-8',
            timeout=1800  # 30分タイムアウト
        )

        if result.returncode == 0:
            self.log("  " + result.stdout.replace('\n', '\n  '))
            self.log("✅ Step 3: 完了")
        else:
            self.log(f"❌ Step 3: 失敗")
            self.log(f"  エラー: {result.stderr}")
            raise Exception(f"Step 3 失敗: {result.stderr}")

    except subprocess.TimeoutExpired:
        self.log("❌ Step 3: タイムアウト（30分以上）")
        raise Exception("Step 3がタイムアウトしました")
    except Exception as e:
        self.log(f"❌ Step 3: エラー - {str(e)}")
        raise
```

---

## 4.3 テストケース

### 4.3.1 正常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-4.1: 連絡先登録成功 | 有効な連絡先CSV | GAMコマンドで連絡先登録成功 |
| TC-4.2: 複数連絡先 | 50件の連絡先CSV | すべて登録成功 |

### 4.3.2 異常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-4.3: 認証エラー | GAM未認証 | エラーメッセージ表示、処理中断 |
| TC-4.4: タイムアウト | 大量データ | タイムアウト例外発生 |

---

# Phase 5: Step 4 - ラベル作成

## 5.1 概要
ラベルCSVから重複を除去し、ユニークなラベルのみをGAMコマンドで作成する。Gitbash経由で実行。

**重複除去仕様**:
- CSV全体からすべてのラベル名を収集
- set()を使用してユニークなラベル名のみを抽出
- "* myContacts" およびシステムラベルを除外
- 各ユニークラベルに対して1回だけGAMコマンドを実行

---

## 5.2 メソッド設計

### 5.2.1 `execute_step4(self)`
**責務**: Step 4: ラベル作成を実行（重複除去対応）

**処理フロー**:
1. ラベルCSVを読み込み（csv.DictReaderを使用）
2. すべての列からラベル名を収集
3. "* myContacts"とシステムラベルを除外
4. "* "プレフィックスを削除
5. set()で重複を除去
6. ユニークなラベル名をソート
7. 各ラベルに対してGAMコマンドを実行
8. 成功/スキップ/エラー件数を集計

**実装コード**:
```python
def execute_step4(self):
    """Step 4: ラベル作成（重複除去対応）"""
    self.log("--- Step 4: ラベル作成 開始 ---")

    try:
        self.log(f"  ラベルCSV: {self.intermediate_label_csv}")
        self.log(f"  ターゲットメール: {self.target_email.get()}")

        # CSVを読み込み、すべてのラベル名を収集
        import csv
        unique_labels = set()

        with open(self.intermediate_label_csv, 'r', encoding='utf-8') as f:
            reader = csv.DictReader(f)
            for row in reader:
                # 各列からラベル名を収集
                for column_name, label_name in row.items():
                    if not label_name or not label_name.strip():
                        continue

                    label_name = label_name.strip()

                    # "* myContacts"は除外（システムラベル）
                    if label_name == '* myContacts':
                        continue

                    # "* "プレフィックスを削除
                    if label_name.startswith('* '):
                        label_name = label_name[2:].strip()

                    # 空でないラベル名のみ追加
                    if label_name and label_name != 'myContacts':
                        unique_labels.add(label_name)

        if not unique_labels:
            self.log("  警告: ラベルが見つかりませんでした")
        else:
            self.log(f"  検出されたユニークなラベル: {len(unique_labels)}個")

            # ラベル名をソートして表示
            sorted_labels = sorted(unique_labels)
            for label in sorted_labels:
                self.log(f"    - {label}")

            # 各ユニークなラベルに対してラベル作成コマンドを実行
            success_count = 0
            skip_count = 0

            for label_name in sorted_labels:
                # GAMコマンド構築
                gam_cmd = (
                    f'gam user {self.target_email.get()} '
                    f'create contactgroup name "{label_name}"'
                )

                self.log(f"  作成中: {label_name}")

                # Gitbash経由で実行
                result = self.run_gam_via_gitbash(gam_cmd)

                if result and result.strip():
                    result_lower = result.lower()
                    if 'error' in result_lower or 'failed' in result_lower:
                        # エラーだが、既に存在する場合はスキップ
                        if 'already exists' in result_lower or '既に存在' in result_lower:
                            self.log(f"    スキップ（既存）: {label_name}")
                            skip_count += 1
                        else:
                            self.log(f"    ⚠️ エラー: {result.strip()}")
                    else:
                        self.log(f"    ✅ 作成完了: {label_name}")
                        success_count += 1

            self.log(f"  ラベル作成完了（成功: {success_count}個, スキップ: {skip_count}個）")

        self.log("✅ Step 4: 完了")

    except Exception as e:
        self.log(f"⚠️ Step 4: 一部失敗の可能性 - {str(e)}")
        # ラベル作成は一部失敗しても処理を継続
        self.log("  処理を継続します")
```

---

## 5.3 テストケース

### 5.3.1 正常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-5.1: ラベル作成成功 | 有効なラベルCSV | GAMコマンドでラベル作成成功 |
| TC-5.2: 日本語ラベル名 | 日本語を含むラベル | 文字化けせず作成成功 |
| TC-5.3: 重複ラベル除去 | 同じラベル名が複数行に存在 | set()で重複除去、1回のみ作成 |
| TC-5.4: "* "プレフィックス処理 | "* Project X" などのラベル | プレフィックスを削除して作成 |

### 5.3.2 異常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-5.5: 既存ラベル | 既存ラベルと同名 | "already exists"を検知、スキップカウント |

---

# Phase 6: Step 5 - 登録データ取得

## 6.1 概要
登録済み連絡先とラベルの一覧をGAMコマンドでCSV出力する。Gitbash経由で実行。

---

## 6.2 メソッド設計

### 6.2.1 `execute_step5(self)`
**責務**: Step 5: 登録データ取得を実行

**処理フロー**:
1. GAMコマンドで連絡先一覧を取得
2. GAMコマンドでラベル一覧を取得
3. UTF-8でファイル保存

**実装コード**:
```python
def execute_step5(self):
    """Step 5: 登録データ取得（Gitbash使用）"""
    from pathlib import Path

    self.log("--- Step 5: 登録データ取得 開始 ---")

    contacts_output = self.work_dir / "contacts.csv"
    labels_output = self.work_dir / "contactgroups.csv"

    try:
        # 連絡先一覧取得（Gitbash経由）
        self.log("  連絡先一覧を取得中...")
        gam_cmd = f'gam user {self.target_email.get()} print contacts'
        contacts_csv = self.run_gam_via_gitbash(gam_cmd)

        with open(contacts_output, 'w', encoding='utf-8', newline='') as f:
            f.write(contacts_csv)

        self.log(f"  ✅ 連絡先一覧: {contacts_output}")

        # ラベル一覧取得（Gitbash経由）
        self.log("  ラベル一覧を取得中...")
        gam_cmd = f'gam user {self.target_email.get()} print contactgroups'
        labels_csv = self.run_gam_via_gitbash(gam_cmd)

        with open(labels_output, 'w', encoding='utf-8', newline='') as f:
            f.write(labels_csv)

        self.log(f"  ✅ ラベル一覧: {labels_output}")
        self.log("✅ Step 5: 完了")

        self.intermediate_contacts_csv = contacts_output
        self.intermediate_contactgroups_csv = labels_output

    except Exception as e:
        self.log(f"❌ Step 5: 失敗 - {str(e)}")
        raise
```

---

## 6.3 テストケース

### 6.3.1 正常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-6.1: データ取得成功 | 有効なターゲットメール | contacts.csvとcontactgroups.csvが生成される |
| TC-6.2: 日本語ラベル | 日本語ラベル存在 | 文字化けせずCSV出力 |

### 6.3.2 異常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-6.3: 認証エラー | GAM未認証 | エラーメッセージ表示、処理中断 |

---

# Phase 7: Step 6 - データ突合

## 7.1 概要
エクスポートデータと登録データを突合し、ラベル登録用のCSVをラベル数ごとに分割して生成する。既存の`main.py`を呼び出す。

**ファイル分割仕様**:
- ラベル数ごとに個別のCSVファイルを生成
- ファイル名形式: `contacts_labels_1.csv`, `contacts_labels_2.csv`, `contacts_labels_3.csv`...
- 各ファイルには対応するラベル数の列のみ含める
- データ処理でラベル数ごとにグループ化

---

## 7.2 メソッド設計

### 7.2.1 `execute_step6(self)`
**責務**: Step 6: データ突合を実行（ラベル数ごとに分割されたCSV生成）

**処理フロー**:
1. Pythonスクリプト実行
2. 引数として各CSVファイルを渡す
3. 複数のCSVファイルが生成される
4. glob パターンで生成されたファイルを検出
5. `final_labels_csv_list` に保存

**実装コード**:
```python
def execute_step6(self):
    """Step 6: データ突合（ラベル数ごとに分割されたCSV生成）"""
    import subprocess
    from pathlib import Path
    import glob

    self.log("--- Step 6: データ突合 開始 ---")

    try:
        output_csv_base = self.work_dir / "contacts_labels"

        self.log(f"  エクスポートCSV: {self.export_csv_path.get()}")
        self.log(f"  連絡先CSV: {self.intermediate_contact_csv}")
        self.log(f"  contacts.csv: {self.intermediate_contacts_csv}")
        self.log(f"  contactgroups.csv: {self.intermediate_contactgroups_csv}")
        self.log(f"  出力ベース: {output_csv_base}")

        cmd = [
            "python",
            "csvContactsSecond/main.py",
            "--export-data", self.export_csv_path.get(),
            "--registered-data", str(self.intermediate_contact_csv),
            "--label-data", str(self.intermediate_contactgroups_csv),
            "--contacts-data", str(self.intermediate_contacts_csv),
            "--target-email", self.target_email.get(),
            "--output", str(output_csv_base) + ".csv"  # ベース名を渡す
        ]

        result = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            encoding='utf-8',
            timeout=300
        )

        if result.returncode == 0:
            self.log("  " + result.stdout.replace('\n', '\n  '))

            # 生成されたCSVファイルを検出
            pattern = str(self.work_dir / "contacts_labels_*.csv")
            generated_files = sorted(glob.glob(pattern))

            if generated_files:
                self.final_labels_csv_list = [Path(f) for f in generated_files]
                self.log(f"  生成されたCSVファイル: {len(self.final_labels_csv_list)}個")
                for csv_file in self.final_labels_csv_list:
                    self.log(f"    - {csv_file.name}")
            else:
                self.log("  ⚠️ CSVファイルが見つかりませんでした")

            self.log("✅ Step 6: 完了")
        else:
            self.log(f"❌ Step 6: 失敗")
            self.log(f"  エラー: {result.stderr}")
            raise Exception(f"Step 6 失敗: {result.stderr}")

    except subprocess.TimeoutExpired:
        self.log("❌ Step 6: タイムアウト（5分以上）")
        raise Exception("Step 6がタイムアウトしました")
    except Exception as e:
        self.log(f"❌ Step 6: エラー - {str(e)}")
        raise
```

---

## 7.3 テストケース

### 7.3.1 正常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-7.1: データ突合成功 | 有効な各CSV | contacts_labels_N.csv（複数）が生成される |
| TC-7.2: ラベル数2種類 | ラベル数2と3の連絡先 | contacts_labels_2.csv, contacts_labels_3.csv が生成される |

### 7.3.2 異常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-7.3: データ不一致 | 一致しないデータ | スキップリストCSV生成、処理継続 |

---

# Phase 8: Step 7 - ラベル登録

## 8.1 概要
複数のcontacts_labels_N.csvファイルを使用して、各連絡先にラベルを登録する。Gitbash経由で実行。

**複数CSV処理仕様**:
- `final_labels_csv_list`から複数のCSVファイルを順次処理
- 各CSVファイルに対してラベル列を自動検出
- ラベル数に応じて動的にcontactgroupパラメータを構築
- 成功/エラー件数を集計

---

## 8.2 メソッド設計

### 8.2.1 `execute_step7(self)`
**責務**: Step 7: ラベル登録を実行（複数CSVファイル対応）

**処理フロー**:
1. `final_labels_csv_list`をチェック
2. 各CSVファイルに対してループ処理:
   - ヘッダー行を読み取り
   - ラベル列を自動検出（PrimaryLabel, SecondaryLabel, ThirdLabel...）
   - contactgroupパラメータを動的に構築
   - GAMコマンド実行
3. 成功/エラー件数を集計
4. サマリーログ表示

**実装コード**:
```python
def execute_step7(self):
    """Step 7: ラベル登録（複数CSVファイル対応）"""
    import csv

    self.log("--- Step 7: ラベル登録 開始 ---")

    try:
        if not self.final_labels_csv_list:
            self.log("  ⚠️ ラベル登録用CSVファイルが見つかりません")
            return

        self.log(f"  処理するCSVファイル数: {len(self.final_labels_csv_list)}")

        success_count = 0
        error_count = 0

        # 各CSVファイルに対してGAMコマンドを実行
        for csv_file in self.final_labels_csv_list:
            self.log("")
            self.log(f"  処理中: {csv_file.name}")

            try:
                # CSVヘッダー行を読み取り、ラベル列を自動検出
                with open(csv_file, 'r', encoding='utf-8') as f:
                    reader = csv.reader(f)
                    headers = next(reader)
                    # データ行数を数える
                    row_count = sum(1 for _ in reader)

                # すべてのラベル列を検出（PrimaryLabel含む、TargetEmail/ContactIDは除外）
                label_columns = [
                    col for col in headers
                    if col.endswith('Label')
                    and col not in ['TargetEmail', 'ContactID']
                ]

                if not label_columns:
                    self.log(f"    ⚠️ ラベル列が見つかりませんでした: {csv_file.name}")
                    error_count += 1
                    continue

                self.log(f"    対象レコード数: {row_count}件")
                self.log(f"    ラベル列: {', '.join(label_columns)}")

                # contactgroupパラメータを動的に構築（各列ごとにcontactgroupを追加）
                contactgroup_params = ' '.join(
                    [f'contactgroup "~{col}"' for col in label_columns]
                )

                # GAMコマンド構築
                gam_cmd = (
                    f'gam csv "{csv_file}" '
                    f'gam user "~TargetEmail" '
                    f'update contacts "~ContactID" '
                    f'{contactgroup_params}'
                )

                self.log(f"    実行: {gam_cmd}")

                # Gitbash経由で実行
                result = self.run_gam_via_gitbash(gam_cmd)

                if result and result.strip():
                    self.log("    GAM出力: " + result.replace('\n', '\n    '))

                self.log(f"    ✅ 完了: {csv_file.name}")
                success_count += 1

            except Exception as e:
                self.log(f"    ❌ エラー: {csv_file.name} - {str(e)}")
                error_count += 1

        self.log("")
        self.log(f"  処理結果: 成功 {success_count}件, エラー {error_count}件")
        self.log("✅ Step 7: 完了")

    except Exception as e:
        self.log(f"⚠️ Step 7: エラー - {str(e)}")
        self.log("  処理を継続します")
```

---

## 8.3 テストケース

### 8.3.1 正常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-8.1: ラベル登録成功（1ファイル） | contacts_labels_2.csv（1ファイル） | GAMコマンドでラベル登録成功 |
| TC-8.2: ラベル登録成功（複数ファイル） | contacts_labels_2.csv, contacts_labels_3.csv | 両ファイルとも登録成功 |
| TC-8.3: 動的列検出 | ThirdLabel, FourthLabel等を含むCSV | ラベル列を自動検出、登録成功 |

### 8.3.2 異常系

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| TC-8.2: 一部失敗 | 存在しない連絡先ID | 警告ログ表示、処理継続 |

---

## 付録A: 全体処理フロー（`run_all_steps()`）

**責務**: 全ステップを順次実行

**実装コード**:
```python
def run_all_steps(self):
    """全ステップを順次実行"""
    import threading

    # ボタン無効化
    self.process_button.config(state=tk.DISABLED, text="実行中...")

    # 別スレッドで実行
    thread = threading.Thread(target=self.process_worker)
    thread.daemon = True
    thread.start()

def process_worker(self):
    """処理ワーカースレッド"""
    from datetime import datetime
    from pathlib import Path

    try:
        # 作業ディレクトリ作成
        run_id = datetime.now().strftime('%Y%m%d_%H%M%S')
        self.work_dir = Path(f"./runs/{run_id}")
        self.work_dir.mkdir(parents=True, exist_ok=True)

        self.log("=" * 60)
        self.log(f"処理開始: {run_id}")
        self.log(f"作業ディレクトリ: {self.work_dir}")
        self.log("=" * 60)
        self.log("")

        # Step 2: CSV抽出
        if self.step2_enabled.get():
            self.execute_step2()
            self.log("")

        # Step 3: 連絡先登録
        if self.step3_enabled.get():
            self.execute_step3()
            self.log("")

        # Step 4: ラベル作成
        if self.step4_enabled.get():
            self.execute_step4()
            self.log("")

        # Step 5: 登録データ取得
        if self.step5_enabled.get():
            self.execute_step5()
            self.log("")

        # Step 6: データ突合
        if self.step6_enabled.get():
            self.execute_step6()
            self.log("")

        # Step 7: ラベル登録
        if self.step7_enabled.get():
            self.execute_step7()
            self.log("")

        self.log("=" * 60)
        self.log("✅ すべての処理が完了しました")
        self.log("=" * 60)

        # 完了ダイアログ
        self.root.after(0, lambda: messagebox.showinfo(
            "完了",
            "すべての処理が正常に完了しました！"
        ))

    except Exception as e:
        self.log(f"❌ エラー: {str(e)}")
        self.root.after(0, lambda: messagebox.showerror(
            "エラー",
            f"処理中にエラーが発生しました:\n{str(e)}"
        ))

    finally:
        # ボタン有効化
        self.root.after(0, lambda: self.process_button.config(
            state=tk.NORMAL,
            text="処理開始 ▶"
        ))
```

---

## 付録B: メインエントリーポイント

**実装コード**:
```python
def main():
    """メイン処理"""
    import tkinter as tk

    root = tk.Tk()
    app = TransferContactsMasterGUI(root)
    root.mainloop()

if __name__ == '__main__':
    main()
```

---

**作成日**: 2025-12-30
**バージョン**: 1.0
**作成者**: Claude Sonnet 4.5


